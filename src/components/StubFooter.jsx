import React, {Component, PropTypes} from 'react';

class StubFooter extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    // const {myBeautifulProps} = this.props;

    return (
      <div>
      </div>
    );
  }
}

StubFooter.propTypes = {
  //Some props
}

export default StubFooter;
